import styles from './Footer.module.scss';

const Footer = () => {
    return (
        <div className={styles.footer}>
            <p className={styles.footer__text}>Mushabüm | Made by Shanice using React and Javascript |</p>
        </div>
    );
};

export default Footer;